#include<stdio.h>
#include<sleep.h>

extern unsigned long ctr1000;

double log(double x);
double pow(double x, int y);
double expdev(double lambda);

double expdev(double lambda){

	int max = 077777;
	double dummy=0.0;
	do{
 	    dummy = (double) (((ctr1000*1103515245 + 12345)>>16)&077777)/(max);
	}while(dummy==0);
	return -log(dummy)/lambda;
}

double log(double x){
	int i;
	double ans=0.0;
	for(i = 1;i<=30;i++){
	    ans = ans + (pow(x-1,i))*(pow(-1.0,i+1))/i;
	}
	kprintf("ans val: %lu\n",ans*10);
	return -ans;
}

double pow(double x, int y){
	while(y>1){
	    x = x*x;
	    y--;
	}
	return x;
}


     

